# API Configuration (if connecting to a backend)
VITE_API_URL=http://localhost:3000

# Lead capture CRM endpoint (defaults to https://api.deepsearch.com/leads/capture)
VITE_LEADS_API_URL=https://api.deepsearch.com/leads/capture

# Enable debug mode (optional)
VITE_DEBUG=false
